{
  "compilerOptions": {
    "composite": true,
    "esModuleInterop": true,
    "module": "CommonJS",
    "moduleResolution": "node",
    /* NOTE: This must be synchronized with the `moduleNameMapper` option in `jest.config.packages.json` */
    "paths": {
      "@metamask/*": ["../*/src"]
    },
    /* We can't enable noEmit because when our subpackages refer to other packages TypeScript will give us a warning */
    /* "noEmit": true, */
    "strict": true,
    "target": "es6"
  }
}
