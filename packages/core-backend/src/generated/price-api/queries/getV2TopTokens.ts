/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import { assert } from '@metamask/superstruct';
import type {
  FetchQueryOptions,
  QueryFunctionContext,
} from '@tanstack/query-core';

import type {
  ApiRequestClient,
  FetchOptions,
} from '../../../api/query-runtime';
import { getQueryOptionsOverrides } from '../../../api/query-runtime';
import { GetV2TopTokensQueryResponseStruct } from '../schemas/getV2TopTokensStruct';
import type {
  GetV2TopTokensQueryResponse,
  GetV2TopTokensPathParams,
  GetV2TopTokensQueryParams,
} from '../types/GetV2TopTokens';

/**
 * Query key for `GET /v2/chains/{chainId}/top-tokens`.
 */
export const getV2TopTokensQueryKey = (
  pathParams: GetV2TopTokensPathParams,
  params?: GetV2TopTokensQueryParams,
) =>
  [
    'prices',
    'getV2TopTokens',
    pathParams,
    ...(params ? [params] : []),
  ] as const;

/**
 * Get top tokens by market cap
 * Returns the top tokens on a chain, ordered by market cap.
 *
 * Returns the TanStack Query options for `GET /v2/chains/{chainId}/top-tokens`. The response is
 * validated against {@link GetV2TopTokensQueryResponseStruct} before it is returned.
 */
export function getV2TopTokensQueryOptions(
  client: ApiRequestClient,
  pathParams: GetV2TopTokensPathParams,
  params?: GetV2TopTokensQueryParams,
  options?: FetchOptions,
): FetchQueryOptions<GetV2TopTokensQueryResponse> {
  return {
    queryKey: getV2TopTokensQueryKey(pathParams, params),
    queryFn: async ({ signal }: QueryFunctionContext) => {
      const data = await client.request<GetV2TopTokensQueryResponse>({
        method: 'get',
        url: `/v2/chains/${pathParams.chainId}/top-tokens`,
        params,
        signal,
      });
      assert(data, GetV2TopTokensQueryResponseStruct);
      return data;
    },
    ...getQueryOptionsOverrides(options),
  };
}

/**
 * Get top tokens by market cap
 * Returns the top tokens on a chain, ordered by market cap.
 *
 * Fetches `GET /v2/chains/{chainId}/top-tokens` through the client's QueryClient, deduplicating
 * concurrent calls and reusing fresh cached data.
 */
export async function fetchV2TopTokens(
  client: ApiRequestClient,
  pathParams: GetV2TopTokensPathParams,
  params?: GetV2TopTokensQueryParams,
  options?: FetchOptions,
): Promise<GetV2TopTokensQueryResponse> {
  return client.queryClient.fetchQuery(
    getV2TopTokensQueryOptions(client, pathParams, params, options),
  );
}
