/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import { assert } from '@metamask/superstruct';
import type {
  FetchQueryOptions,
  QueryFunctionContext,
} from '@tanstack/query-core';

import type {
  ApiRequestClient,
  FetchOptions,
} from '../../../api/query-runtime';
import { getQueryOptionsOverrides } from '../../../api/query-runtime';
import { GetV3SpotPricesQueryResponseStruct } from '../structs/getV3SpotPricesStruct';
import type {
  GetV3SpotPricesQueryResponse,
  GetV3SpotPricesQueryParams,
} from '../types/GetV3SpotPrices';

/**
 * Query key for `GET /v3/spot-prices`.
 */
export const getV3SpotPricesQueryKey = (params: GetV3SpotPricesQueryParams) =>
  ['prices', 'getV3SpotPrices', params] as const;

/**
 * Get spot prices by CAIP-19 asset IDs
 * Returns a list of spot prices for a given list of CAIP-19 asset IDs.
 *
 * Returns the TanStack Query options for `GET /v3/spot-prices`. The response is
 * validated against {@link GetV3SpotPricesQueryResponseStruct} before it is returned.
 */
export function getV3SpotPricesQueryOptions(
  client: ApiRequestClient,
  params: GetV3SpotPricesQueryParams,
  options?: FetchOptions,
): FetchQueryOptions<GetV3SpotPricesQueryResponse> {
  return {
    queryKey: getV3SpotPricesQueryKey(params),
    queryFn: async ({ signal }: QueryFunctionContext) => {
      const data = await client.request<GetV3SpotPricesQueryResponse>({
        method: 'get',
        url: '/v3/spot-prices',
        params,
        signal,
      });
      assert(data, GetV3SpotPricesQueryResponseStruct);
      return data;
    },
    ...getQueryOptionsOverrides(options),
  };
}

/**
 * Get spot prices by CAIP-19 asset IDs
 * Returns a list of spot prices for a given list of CAIP-19 asset IDs.
 *
 * Fetches `GET /v3/spot-prices` through the client's QueryClient, deduplicating
 * concurrent calls and reusing fresh cached data.
 */
export async function fetchV3SpotPrices(
  client: ApiRequestClient,
  params: GetV3SpotPricesQueryParams,
  options?: FetchOptions,
): Promise<GetV3SpotPricesQueryResponse> {
  return client.queryClient.fetchQuery(
    getV3SpotPricesQueryOptions(client, params, options),
  );
}
